#include "main.h"

/**
* rot13 -  encodes a string using rot13
* @str:the string targeted
*Return: returns the encoded string
*/

char *rot13(char *s)
{
	int i;

	while (s[i] != '\0')
	{
		if (i = 0; ((i >= 'a' && i <= 'z')||(i >= 'A' && i <= 'Z'));   
