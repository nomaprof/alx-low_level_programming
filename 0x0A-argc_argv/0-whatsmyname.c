#include "main.h"

/**
* main - whats the program name
*@argc: number of command line arguments
*@argv: the place the arguments are stored
*Return: 0
*/

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
