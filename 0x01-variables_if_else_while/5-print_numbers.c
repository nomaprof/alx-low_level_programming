#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int base10;

	for (base10 = 0; base10 < 10; base10++)
	{
		printf("%d", base10);
	}
	printf("\n");
	return (0);
}
